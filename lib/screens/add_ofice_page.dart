import 'package:flutter/material.dart';

class AddOficePage extends StatelessWidget {
  const AddOficePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('اضافة مكتب'),
      ),
    );
  }
}